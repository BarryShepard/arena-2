{
  "name": "arena-file-mods",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "test": "node --test --test-concurrency=1 tests/*.test.js",
    "test:browser": "node tests/browser-smoke.mjs"
  },
  "dependencies": {
    "quickjs-emscripten": "^0.32.0",
    "vite": "^8.3.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "prettier": "^3.9.6"
  }
}
